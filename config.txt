#延迟时间 , 时间以ms为单位
#下单程序窗口标题名
#照着原来格式修改参数即可，其他的不要乱动

切换到下单程序之后的延迟="1000"
按下F1(F2)之后的延迟="1000"
按下F2(F1)之后的延迟="1000"
输入股票代码各数字之间的延迟="100"
输完股票代码之后的延迟="200"
切换到卖出(买入)价格按下的TAB键之后的延迟="500"
按五次退格键删掉预设价格(数量)之后的延迟="500"
输入跌(涨)停价之后的延迟="500"
切换到卖出(买入)数量按下的TAB键之后的延迟="500"
输完卖出(买入)数量之后的延迟="500"
切换到卖出(买入)按钮按下的TAB键之后的延迟="500"
按下卖出按钮之后的延迟="500"
按下确认卖出的ENTER键之后的延迟="500"
按下最后一次ENTER键之后的延迟="1500"
每次查询间隔时间="3000"
清空信息框间隔时间="1800000"


下单程序名字="网上股票交易系统"
